# data/processed — feature datasets (regenerated by the pipeline)
`merged_dataset.csv` (Task 1, from data_merge.py), `image_features.csv` (Task 2), `audio_features.csv` (Task 3). Required deliverables — committed once the pipeline runs on the full team data.
